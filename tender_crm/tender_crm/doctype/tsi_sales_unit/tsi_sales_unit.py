# A simple lookup: the sales team split TSI's Clients/Leads screens filter and
# group by, distinct from CRM Territory (which is geography, not team).

from frappe.model.document import Document


class TSISalesUnit(Document):
    pass
